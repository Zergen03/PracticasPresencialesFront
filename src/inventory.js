var inventoryContainer = document.querySelector('.contenedor__inventario')

document.addEventListener('DOMContentLoaded', async function () {
    console.log(inventoryContainer);
    
    findItems(1, inventoryContainer);
});



